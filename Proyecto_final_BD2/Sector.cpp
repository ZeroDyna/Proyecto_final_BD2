#include "Sector.h"

Sector::Sector(size_t size, std::vector<Tablas> tabla_) {
	std::cin >> size;
	tabla = tabla_;
}
bool Sector::insertar_datos(std::vector<char> test) {
	//acá quiero hacer que el lenguaje de cconsulta haga un insert
	//se acabo el chatgpt
	
}